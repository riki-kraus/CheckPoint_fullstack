import { SchemaDescription, ObjectSchema, AnyObject } from 'yup';
import { AllFieldProps, FieldProps } from '../types';
export declare const getFieldPropsFromDescription: <T extends FieldProps = AllFieldProps>({ name, fieldDescription, values, context, throwError, }: {
    name: string;
    fieldDescription: SchemaDescription;
    values: any;
    context?: AnyObject;
    throwError?: boolean;
}) => T;
export declare const getFieldProps: <T extends FieldProps = AllFieldProps>({ name, schema, values, context, throwError, }: {
    name: string;
    schema: ObjectSchema<any, AnyObject, any, "">;
    values: any;
    context?: AnyObject;
    throwError?: boolean;
}) => T;
