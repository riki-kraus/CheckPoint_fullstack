import { AnyObject } from 'yup';
type ResolveRefProps = {
    param: any;
    values: any;
    name: string;
    context?: AnyObject;
};
export declare const resolveRef: ({ param, values, name, context, }: ResolveRefProps) => any;
export declare const resolveRefForExtractor: <T>({ param, values, name, context, key, typeCheck, toStrictType, }: ResolveRefProps & {
    key: string;
    typeCheck?: string | ((value: unknown) => boolean);
    toStrictType?: (value: unknown) => T;
}) => {
    [x: string]: T;
};
export {};
