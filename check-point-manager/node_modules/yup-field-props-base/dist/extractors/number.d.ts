import { ExtractorArgs } from './types';
export declare const extractNumberValidationFromTest: ({ test, values, name, context, }: ExtractorArgs) => {
    [x: string]: number;
} | {
    integer: boolean;
} | null;
